# Production environment variables
# These are baked into the build at compile time by Vite.
# Replace with the actual production middleware URL before deploying.

VITE_API_URL=https://api.yourdomain.com/api
